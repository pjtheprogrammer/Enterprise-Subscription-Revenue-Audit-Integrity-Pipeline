Designed and built an end-to-end data pipeline leveraging Python (Pandas) and PostgreSQL to detect, cleanse, and quantify reporting inflation caused by upstream CRM/Billing system synchronization failures.

Engineered an automated Python preprocessing script to extract source data, enforce data integrity rules by filtering out negative revenue anomalies, and programmatically apply date placeholders to optimize downstream time-series math.

Formulated advanced PostgreSQL logic to decouple financial reporting, engineering a production view for true Monthly Recurring Revenue (MRR) alongside an audit view to isolate historical "ghost revenue."

Optimized system performance by shifting heavy conditional evaluation from the presentation layer (Power BI) to the backend database engine, minimizing DAX architectural complexity and significantly reducing report load times.

Created executive-ready dashboards delivering actionable, dollar-mapped insights for financial teams to audit operational system bugs and reconcile cumulative paper leakage.
